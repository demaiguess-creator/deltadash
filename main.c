/* Delta Dash NES - simple square + coins game in C */
#include <nes.h>

unsigned char playerX = 10;
unsigned char playerY = 10;

void main(void) {
    while(1) {
        /* simple infinite loop; movement & game logic can be added */
    }
}
